import { Typography, Container } from '@mui/material';

function Events() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Events
      </Typography>
      <Typography variant="body1" color="text.secondary">
        Events timeline page - To be implemented
      </Typography>
    </Container>
  );
}

export default Events;
